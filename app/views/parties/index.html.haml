#parties
  - @parties.each do |party|
    .party
      %h2= link_to party.titre, party
      %p= party.prix
      %p= party.date
      %p= party.description
      %p= party.nombreparticipants

    / = link_to "Nouvelle soirée", new_party_path
    / = link_to "Editer", edit_party_path(party.id)
    / = link_to "Supprimer", party_path(party.id), method: :delete, data: { confirm: " Sarah Connor ?  Oui ?? 'PAN' "}
